# web3min progress

## Status
- [x] Langkah 1: Pengaturan — hapus Host ID, region, ping Supabase dari UI; label diubah jadi "Sinkronisasi progres" (commit `10f7f88`).
- [x] Langkah 2: Audit RLS Supabase semua tabel (Selesai diaudit & dilaporkan — fail-closed, no service key in frontend).
- [x] Langkah 3: Sembunyikan & filter akun test (sectest_*, testuser99) dari leaderboard & RPC (`rpcGetLeaderboard` filtering + migration).
- [x] Langkah 4: Hapus akun yang menghapus data di Supabase (`/api/auth/delete-account`, `rpcDeleteMyAccount`, modal konfirmasi di `/settings`).
- [x] Langkah 6/Bug 3: Bug /kisah/s-dm dan deep link /kisah/[id] — membuka cerita/preview langsung tanpa redirect paksa ke onboarding/home.
- [x] Langkah 8: Email pemulihan akun + reset password OTP 6-digit via Resend (endpoint `/api/auth/request-reset` & `/confirm-reset`, kartu di `/profile`, modal "Lupa password?" di `/masuk`). Domain email dibatasi allowlist penyedia publik (Gmail/Yahoo/Outlook/iCloud/Proton); temp-mail & domain bisnis ditolak di client + server (`src/lib/account.ts` `isValidRecoveryEmail`, `server/api/auth/request-reset.post.ts`).
- [x] Langkah 5 (SEO/PWA): `public/robots.txt` + `public/sitemap.xml` (153 URL digenerate dari sumber konten via `scripts/generate-sitemap.ts`, ikut jalan di `npm run build`). HTML awal nyata untuk `/about`, `/cara`, `/kisah`, `/` — `HydrationGate` tidak lagi mengganti seluruh halaman dengan `<BootScreen/>`; boot screen jadi overlay. Bukti: body text /about 72 → 1.162 char, /cara 1.607, /kisah 3.481, `/` 3.586.
- [x] Langkah 7a (PWA & OG): manifest ikon 192 & 512px (`icon-192.png`, `icon-512.png` di-generate dari favicon, `purpose: any + maskable`), `<link rel=icon>` 192/512 di head. og:image disatukan ke satu sumber `https://web3min.com/og.jpg` (`DEFAULT_OG_IMAGE` = `src/lib/og/site.json`); `og-image.png` duplikat dihapus.
- [x] Langkah 7b (Performa/Tipografi): budget font terpenuhi. `styles.css` tidak lagi `@import` Google Fonts (dulu 2 request); satu `<link>` di `__root.tsx`. Dibuang: JetBrains Mono (→ system mono), Silkscreen (0 pemakaian), Press Start 2P (0 pemakaian), Nunito (0 pemakaian). Bukti runtime: `googleCssRequests: 1`, `bodyFontSize: 16px`. Guard test: `src/lib/font-budget.test.ts`.
- [x] Langkah 7c (Hapus mode Retro Pixel — permintaan langsung): toggle tipografi 8-bit dibuang seluruhnya. (1) `src/components/top-status.tsx`: pill toggle `PIXEL`/`MODERN` di header dihapus. (2) `src/routes/settings.tsx`: kartu "Mode Tipografi Retro Pixel" dihapus. (3) `src/lib/store.ts`: state `pixelMode` + aksi `setPixelMode` dihapus (type, initial, sanitize, setter) — 4 titik. (4) `src/components/hydration-gate.tsx`: `applyPixelMode()` dihapus. (5) `src/styles.css`: `--font-pixel` diarahkan ke `var(--font-display)` (392 pemakaian `.font-pixel` tetap bekerja tanpa satu pun call site diubah); blok `html[data-pixel-mode="true"], .pixel-mode` **DIHAPUS** karena satu-satunya penyala `applyPixelMode()` sudah tiada sehingga selector-nya tidak pernah cocok; penimpa 8-bit `html[data-pixel-mode="true"] h1..h4` dihapus. (6) `src/routes/__root.tsx`: `Pixelify+Sans` dibuang dari `<link>` → **2 keluarga** (Bricolage Grotesque display, Plus Jakarta Sans body). (7) `src/components/ui/8bit/styles/retro.css` **DIHAPUS** — ternyata dead file (nol importer) dan satu-satunya berkas yang masih benar-benar merujuk `font-family: "Pixelify Sans"`. (8) `src/lib/font-budget.test.ts`: guard diperketat — budget 3 → **2**, dan gagal kalau `Pixelify` kembali muncul di `<link>` atau di token `--font-*`. Bukti runtime (Playwright): badge `MODERN`/`PIXEL` hilang dari header, `data-pixel-mode` tidak pernah disetel, **0 request ke Pixelify**, hanya `bricolagegrotesque` + `plusjakartasans` yang terunduh, 4 halaman (`/cara`, `/about`, `/privacy`, `/kisah`) PASS dengan heading font = Bricolage Grotesque, exit 0. Statis: 70/70 test pass, `build` exit 0.
- [x] Langkah 7d (Hapus pill toggle suara di header — permintaan langsung): pill speaker `Volume2`/`VolumeX` di `TopStatus` dibuang. Satu-satunya kontrol efek suara sekarang kartu "Efek Suara" (Aktif/Mute) di `/settings`. Dicek dulu sebelum menghapus, karena `/settings` cuma memanggil `setSound()` dan sekilas tampak tidak menyentuh mesin audio — ternyata `src/routes/__root.tsx:21` sudah menjalankan `setAudioEnabled(sound)` reaktif, jadi pill header hanya jalan pintas duplikat dan tidak ada fungsi yang hilang. Import yatim dibersihkan (`Volume2`, `VolumeX`, `setAudioEnabled`, `setSound`). Bukti runtime (Playwright 320/360/390/430px): pill hilang di 4 lebar, overflow horizontal 0, header tetap 56px, tap target lain tidak rusak; toggle `/settings` diuji klik nyata `"Aktif"` → `"Mute"` dan masih berfungsi. Guard baru di `font-budget.test.ts` (komentar dibuang dulu sebelum assert, supaya menyebut nama ikon di komentar tidak memicu gagal palsu). 71/71 test pass.
- [x] Langkah 6a (UI/Mobile): overflow horizontal 12px di lebar 360px (header `TopStatus` — brand + 4 pill) diperbaiki. Sebelum: `scrollWidth 372` vs `clientWidth 360` (juga terjadi di produksi). Sesudah: `overflow 0` di 360 & 430.
- [x] Langkah 9 (Offline & Error State): (a) `OfflineBanner` global di `__root` (bukan di `AppShell` — 11 rute tidak memakai AppShell) mendengarkan event `online`/`offline`, pesan "Kamu sedang offline. Progres tersimpan di perangkat ini." + tombol "Coba lagi" tap target 44px. (b) `public/sw.js` + `public/offline-shell.html`: service worker menyajikan shell offline untuk request navigasi yang gagal (bukan `ERR_INTERNET_DISCONNECTED`), `/api/` dibalas 503 JSON, aset statis cache-first. (c) `src/components/ui/skeleton.tsx` `SkeletonRows` menggantikan `CandyLoader` di klasemen. (d) `rpcGetLeaderboard` sekarang mengembalikan `ok: boolean` — UI bisa membedakan "klasemen kosong" dari "server tak terhubung"; klasemen error menampilkan kotak error + tombol "Coba lagi" yang benar-benar me-`refetch` (`reloadKey` dependency).
- [x] Langkah 9 — 2 bug runtime ditemukan saat verifikasi produksi lalu diperbaiki (commit `4cb2542`): (1) **retry loop tak berujung** — shell memanggil `location.reload()` saat load kalau `navigator.onLine` true, jadi kalau shell tersaji sementara koneksi hidup (user mengetik `/offline-shell`, atau SW menyajikan cache karena timeout) halaman reload tanpa henti (terbukti: `page.goto` timeout 25.000 ms). Fix: reload hanya saat event `online` menyala setelah benar-benar offline; tombol pakai `location.replace("/")`. (2) **shell 404** — Vercel clean URLs menjawab `/offline-shell.html` dengan 404 (juga `/index.html`, `/prototype/index.html`); `SHELL_URL` diganti ke `/offline-shell` tanpa ekstensi, `VERSION` di-bump ke `w3m-v3`. Bukti produksi: `GET /offline-shell` 200 (identik berkas lokal 3822B), halaman stabil 6s, SW aktif dengan cache `w3m-v3-shell`, banner offline `role=status` + `aria-live=polite`, navigasi offline ke `/wardrobe` menampilkan shell bukan error browser.
- [x] Langkah 6b (Warna Feedback & Kontras): root cause ditemukan lewat audit token — kelas warna yang **diam-diam tidak resolve** karena token belum dideklarasikan di `@theme` (Tailwind v4 hanya emit utility kalau token ada; typo macam `text-choco-700` saat hanya `choco-900/600` ada akan compile tanpa error dan elemen cuma mewarisi warna induk). Dulu 26 token rusak di 307 call site, sekarang **0**. Tambahan: `--color-candy-deep` (dipakai 9× tapi tidak pernah ada), `--color-muted` (30×), `--color-danger-shadow` (dirujuk, tidak didefinisikan), varian `*-ink` yang lolos WCAG AA di atas latarnya sendiri **dan** di atas cream. State `disabled` tombol diperbaiki: dulu `background: --color-line` = `#3B2218` hampir hitam, jadi tombol nonaktif justru terlihat paling menonjol dan labelnya cuma 3.78:1 → sekarang abu netral + `choco-600` (6.28:1). Guard: `src/lib/feedback-colors.test.ts` (token wajib ada) + `src/lib/contrast-budget.test.ts` (ambang kontras).
- [x] Langkah 6b lanjutan — celah kontras CTA **ditutup** (keputusan Thalassa: gelapkan gradient, jangan ubah pink brand). 96 baris di 31 file memindahkan fill berteks putih dari `candy-500` `#E8437F` (3.79:1, gagal AA) ke ramp `candy-800` `#85174A` (9.46:1). `.btn-gummy` di `styles.css` memakai `candy-700/800/950` — sengaja satu langkah lebih gelap dari yang "jelas", karena `hover:brightness-105` + `active:brightness-105` menaikkan tiap stop; worst state 5.60:1. Gradient nav aktif (`CANDY_ACTIVE` di `bottom-nav.tsx`) stop 0% wajib `candy-700`, bukan `candy-600`: label nav 10px putih lolos saat diam (4.71:1) tapi hover `brightness-110` menjatuhkannya ke 4.0:1. Verifikasi: interpolasi oklab penuh (Chromium menginterpolasi gradient di oklab) worst 6.53:1; audit `scripts/audit-cta-contrast.py` = **0** temuan di TSX. Guard: test `no white text sits on --color-candy-500` + test ramp nav (rest + hover).
- [x] Langkah 7 (Navbar pill + tombol Lanjut): navbar bawah pill mengambang, item aktif berlatar pill + label, **tombol bulat pink "Lanjut"** di tengah (commit `f6c61a4`, dikerjakan sesi paralel lalu saya verifikasi runtime). Bukan rute baru — `firstPlayableId(completed)`: ke blok berikutnya yang belum selesai, balik ke beranda kalau semua tuntas. Grid 7 kolom + spacer diganti **6 kolom sama lebar** (dengan 7 kolom elemen ke-3 jatuh ke kolom spacer, label "Arena" menciut jadi 12px). Tombol pakai `size-[min(52px,100%)]` karena `size-13` (52px) meluber di kolom 320px dan menimpa "Toko". Bukti terukur (Playwright 320/360/390/430px): 5 link + 1 tombol, tap target ≥44px, nol overlap, nol overflow. Tombol sengaja ikon saja tanpa teks — label putih di atas pink brand 3.79:1, ikon sebagai objek grafis cukup 3:1; karena itu `aria-label` wajib.
- [x] Langkah 8 (UI/UX Less is More & Mobile Polish): (a) `TipCard` di `src/components/lesson/exercises.tsx` dirombak ke pola akordion bertingkat (Konsep Inti scannable default, Contoh Nyata collapsible, Kunci Ingatan & Jebakan collapsible, hapus diagram Web2/Web3 statis universal). (b) Anti-zoom iOS Safari: seluruh `<input>` form di `/masuk`, `/onboarding`, `/settings`, dan `/profile` distandarisasi ke `text-base sm:text-sm` (>= 16px di mobile) guna mencegah auto-zoom paksa viewport. (c) Keyboard-safe & Safe Area: form `/masuk` disesuaikan (`flex flex-col`, `overflow-y-auto`, `scroll-mb-16`) agar tombol submit tidak tertutup virtual keyboard; header `TopStatus` diberi `pt-[env(safe-area-inset-top)]` agar indikator koin & nyawa aman dari notch/Dynamic Island iPhone. (d) Store-readiness: form pendaftaran `/onboarding` dilengkapi tautan persetujuan Kebijakan Privasi `/privacy`, serta CTA berorientasi hasil (`Masuk Sekarang →`, `Lanjut ke Target Harian →`). (e) Taktil bottom-nav: penambahan `active:scale-95` pada FAB tombol Lanjut.
- [x] Langkah 9 lanjutan (Skeleton State & Human CTAs): (a) `src/components/ui/skeleton.tsx`: penambahan komponen `SkeletonCards` (kartu taktil 2-kolom responsif) menggantikan `CandyLoader` spinner melayang di `src/routes/raffle.tsx` guna mengeliminasi layout-shift saat data undian termuat. (b) Empty state `/raffle`: jika kategori/status undian kosong (`filteredRaffles.length === 0`), menampilkan kartu status informatif + tombol "Lihat Semua Undian →" (bukan layar kosong). (c) Human CTAs: normalisasi tombol aksi pasif — `Klaim` di `daily-quests.tsx` menjadi `Klaim +{q.gems} Koin →`; tombol di `shop.tsx` menjadi `Beli Pelindung →`, `Beli Item →`, dan `Beli Sekarang ({cost} Koin) →`.
- [x] Langkah 10 (Leaderboard Visual Unification & Human Email Verification): (a) `PRD.md` & `DESIGN.md`: standarisasi Single Source of Truth arsitektur visual *Tactile Spatial Arcade* berbasis `/leaderboard`. (b) Rute `/raffle`: sub-navigasi diselaraskan menjadi Arena Sub-Navigation Tabs pill dock terpusat (`max-w-md mx-auto`) identik `/leaderboard`, judul hero diupgrade ke `font-pixel font-bold text-3xl md:text-4xl text-choco-900 tracking-tight`, dan penambahan reciprocal bridge banner emas ke Klasemen Mingguan (`bg-gradient-to-b from-[#FFFBEB] via-[#FEF3C7] to-[#FDE68A]`). (c) Rute `/profile`: merombak kartu Email Pemulihan menjadi "Verifikasi Email Akun", status badge arcade "Terverifikasi ✓", menghapus teks dump 17 string domain teknis dan menggantinya dengan copy ramah manusia "Gunakan email utama (Gmail, Yahoo, Outlook, iCloud, Proton) untuk verifikasi akun.", serta tombol aksi "Verifikasi & Simpan Email →". (d) Verifikasi: 69/69 test suite pass (`npm test` exit 0), Nitro SSR build pass (`npm run build` exit 0).
- [x] Langkah 10 lanjutan (TactileButton Disabled Contrast Fix): Memperbaiki bug tombol disabled di `src/components/ui/tactile-button.tsx` (baris 125-133). Root cause: state disabled menggunakan `bg-line text-faint` di mana `--color-line` bernilai `#3B2218` (cokelat pekat/hampir hitam) dan `--color-faint` tidak dideklarasikan, menghasilkan balok cokelat gelap pekat dengan teks tak terbaca (kontras < 1.2:1) saat input form kosong/disabled. Fix: memasang kelas `.tactile-btn` dan menyelaraskan state disabled ke `bg-[#EDE4DC] text-choco-600 border-2 border-choco-900/25` (sesuai aturan AGENTS.md abu netral 6.28:1). 69/69 test suite pass (`npm test` exit 0).
- [x] Langkah 10 lanjutan (Raffle Hero Card Bento Restructure): Merestrukturisasi tata letak kartu hero `/raffle` di `src/routes/raffle.tsx` yang sebelumnya berantakan (semua elemen kontrol dan tombol di baris horizontal kanan yang sempit hingga tombol panduan terpotong). Perbaikan: (1) Tombol "Panduan Undian" dipindahkan ke pojok kanan atas kartu berdampingan dengan badge siklus aktif. (2) Saldo tiket & koin ditata dalam kartu bento bertumpuk vertikal dengan tombol aksi utama "+ Beli Tiket Sekarang →" di bawahnya selebar kartu saldo (`w-full`), menyamakan tinggi dan baseline visual kiri dan kanan. (3) 69/69 test suite pass (`npm test` exit 0).
- [x] Langkah 10 lanjutan (Raffle Item Cards Neo-Brutalist Bento Refactor): Merombak kartu undian di `src/routes/raffle.tsx` (baris ~780-1030). Perbaikan: (1) Judul di-upgrade ke `font-pixel font-bold text-xl md:text-2xl text-choco-900 leading-tight capitalize tracking-tight` dengan subtitle hadiah warna pink berikon award. (2) Menghapus badge pill overlay `SLOT MINT` yang menutupi grafis karya seni / lowrider agar artwork bisa bernafas bebas. (3) Mengganti 4 kartu statistik terpisah dengan 1 Bento Grid 2x2 solid bergaris pembagi (`divide-x-2 divide-y-2 border-2 border-choco-900 rounded-2xl bg-white shadow-[0_3px_0_#3B2218] overflow-hidden`) yang menampilkan total tiket, kuota pemenang, tiket kamu, dan countdown sisa waktu secara proporsional. (4) Tombol aksi utama "Pasang Tiket Undian →" menggunakan gradien tactile 3-stop candy (`from-[#D62A78] via-[#B01F62] to-[#85174A]`) dengan tinggi seragam `h-11 sm:h-12` dan shadow 3D `shadow-[0_3px_0_#3B2218]`. (5) Seed hash diringkas ke bentuk pill verifikasi monospaced dengan link `Verifikasi ↗`. (6) 69/69 unit tests pass (`npm test` exit 0), Vite + Nitro SSR build pass (`npm run build` exit 0).
- [x] Langkah 10 lanjutan (Universal Master Design Unification across SurfaceCard & Shop): Menyelaraskan seluruh sistem kartu dan rute `/shop` ke master design system `/leaderboard`. Perbaikan: (1) `src/components/ui/surface-card.tsx`: Mengubah semua varian (`default`, `cream`, `sky`, `interactive`) dari border pudar 18% ke `border-2 border-choco-900` (100% solid komik) dan bayangan blur ambient diganti menjadi hard 3D extrusion `shadow-[0_4px_0_#3B2218]` dengan `rounded-3xl` (otomatis merapikan seluruh kartu di `/profile`, `/settings`, dll). (2) `src/routes/shop.tsx`: Mode switcher dock ("Toko Blobi" & "Ruang Ganti") diselaraskan menjadi Arena pill dock terpusat `max-w-md mx-auto rounded-2xl border-2 border-choco-900/20 bg-gradient-to-b from-white via-[#FFF9F5] to-[#FDEEE4] shadow-[0_4px_0_#3B2218]`. Tab aktif menggunakan `bg-gradient-to-b from-[#B01F62] via-[#85174A] to-[#6E1239] text-white border-2 border-candy-600/50 shadow-[0_3px_0_#6E1239] font-pixel font-bold`. (3) Seluruh hero banner toko, kartu bento item kuis/streak, dan lemari pakaian di `/shop` menggunakan border solid `border-2 border-choco-900 shadow-[0_6px_0_#3B2218]` dengan tombol pill bertingkat 3-stop candy gradient. (4) 70/70 unit tests pass (`npm test` exit 0), Vite + Nitro SSR build pass (`npm run build` exit 0).
- [x] Langkah 10 lanjutan (Zone Seam Blend / peta pulau tidak lagi patah): Dua pulau bertumpuk bertemu di satu garis batas. Terukur dulu, bukan ditebak: pixel-scan baris sambung menunjukkan lompatan **133-192 satuan RGB dalam satu baris** (garis `patah` keras). ROOT CAUSE: `--seam-color` hanya dipakai untuk memudarkan tepi ATAS pulau berikutnya memakai warna dasar (`theme.bg`) pulau itu; tepi BAWAH pulau sebelumnya tidak disentuh sama sekali, dan warna dasar tema jauh berbeda dari artwork aslinya. FIX: (1) `mixSeamColor()` baru di `src/lib/pulau-rantai.ts` — satu warna bersama (rata-rata dua warna dasar pulau) untuk KEDUA sisi sambungan. (2) `styles.css`: `.world-seam-top` (menempel bawah pulau sebelumnya) + `.world-seam-bottom` (menempel atas pulau berikutnya), keduanya memakai `--seam-color` yang SAMA, dengan bahu solid 12% lalu rumpun gradient `color-mix` yang menghilang. (3) `z-index: 2` — wajib di ATAS overlay pencahayaan atmosferik (`z-1`), kalau tidak overlay menintai sambungan asimetris dan garis kerasnya balik lagi. (4) `backdrop-filter: blur(6px)` di puncak sambungan sebagai kabut kedalaman. BUKTI terukur (Playwright + pixel-scan): lompatan baris sambung **133-192 -> 6-10 satuan RGB**, yaitu setara baris lain di sekitarnya (tidak bisa dibedakan lagi); scan median selebar layar tidak menemukan satu baris pun dengan lompatan seragam. Guard baru: `src/lib/seam-blend.test.ts` (6 test: rata-rata persis, tidak bergantung urutan argumen, hex valid, tahan input rusak, hasil berbeda dari warna mentah). 76/76 test pass, `tsc --noEmit` exit 0, `build` exit 0.

- [ ] Sisa Langkah 8: checklist "Lanjut kalau kamu sudah bisa..." sebelum PETI. Terhalang aturan repo "jangan ubah teks materi/kuis kecuali diminta" — bagian yang belum ada teksnya butuh izin eksplisit untuk diisi.
- [x] ~~Celah kontras CTA putih-di-pink~~ **SUDAH DITUTUP** (lihat Langkah 6b lanjutan). Sisa yang masih benar-benar terbuka: 4 blok CSS legacy (`.ic.sk`, `.bn.now`, `.bubble`, `.btn-candy`) yang masih berisi teks putih di atas `--color-primary`. Dibuktikan **mati**: DOM walk di 7 rute (1440px) menemukan 356 class token, tidak satu pun dari keempatnya. Aman dihapus kapan saja; sengaja tidak disentuh karena di luar scope langkah ini. `scripts/audit-cta-contrast.py` menandainya sebagai `[dead, ignore]` supaya temuan nyata tidak terkubur.
- [ ] Temuan kecil (bukan bug): 11 token warna terdefinisi tapi tidak dirujuk (`--color-brand`, `--color-err-ink`, `--color-ok-soft`, dst) — alias sisa migrasi. Aman dihapus kapan saja, sengaja tidak disentuh di langkah ini.
- [ ] Temuan untuk dicek manusia (Langkah 9 lanjutan, bukan bug baru): React error #418 (hydration text mismatch) HANYA di halaman `/` — juga terjadi di produksi, jadi bukan regresi Langkah 9. `/leaderboard` bersih. Perlu audit terpisah.
- [ ] Sisa kecil: tab klasemen keluar viewport di 360px (`overflow 12px`); dibiarkan karena `<div>` itu memang `overflow-x: auto` (scrollable by design), bukan overflow halaman.
- [ ] Langkah 9 lanjutan (opsional): rute selain klasemen (`admin`, `raffle`) masih pakai `CandyLoader`, belum skeleton.
